name: Build and Publish an injected Docker image
on:
  release:
    types:
      - published

jobs:
  ping_matrix:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch files from release
        run: |
          echo Repo: ${{ github.event.repository.full_name }}

          echo Name: ${{ github.event.release.name }}
          echo Tag: ${{ github.event.release.tag_name }}
          echo Draft: ${{ github.event.release.draft }}
          echo Prerelease: ${{ github.event.release.prerelease }}
          echo Assets: ${{ github.event.release.assets }}

          # collator binary sample
          # https://github.com/paritytech/cumulus/releases/download/statemine-v6.0.0/polkadot-collator

      - name: Check files

      - name: Build injected image

      - name: Tag and Publish
